package com.example.chessclock.ui.theme

import androidx.compose.ui.graphics.Color

/** 页面底色：偏绿的近黑色。 */
val ClockBackground = Color(0xFF0A0E0C)

/** 卡片 / 弹窗底色。 */
val ClockSurface = Color(0xFF141A17)

/** 等待方的卡片底色（配合整体压暗）。 */
val ClockCardIdle = Color(0xFF101613)

/** 当前走棋方的卡片底色（高亮绿色）。 */
val ClockCardActive = Color(0xFF10301F)

/** 当前走棋方的高亮绿色。 */
val ClockGreen = Color(0xFF22C55E)

/** 更亮的绿，用于文字强调。 */
val ClockGreenBright = Color(0xFF4ADE80)

/** 深绿，用于容器色。 */
val ClockGreenDark = Color(0xFF14532D)

/** 读秒阶段的橙色数字。 */
val ClockOrange = Color(0xFFF59E0B)

/** 读秒只剩 5 秒时的红色（闪烁）。 */
val ClockRed = Color(0xFFEF4444)

/** 主要文字颜色。 */
val ClockTextPrimary = Color(0xFFE8F5EE)

/** 次要文字颜色（标签、提示）。 */
val ClockTextSecondary = Color(0xFF8CA396)

/** 非高亮卡片的描边。 */
val ClockBorderIdle = Color(0xFF232D28)
